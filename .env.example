# No env vars required for seed or MCP (list_node_types, get_node_info, etc.).
# Optional: GITHUB_TOKEN for rate limits when using sync-manager (fetches from GitHub).

# ComfyUI API (only for execute_workflow, get_execution_status, list_queue)
# COMFYUI_HOST=http://localhost:8188
